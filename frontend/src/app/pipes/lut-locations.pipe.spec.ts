import { LutLocationsPipe } from './lut-locations.pipe';

describe('LutLocationsPipe', () => {
  it('create an instance', () => {
    const pipe = new LutLocationsPipe();
    expect(pipe).toBeTruthy();
  });
});
