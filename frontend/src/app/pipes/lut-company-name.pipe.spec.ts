import { LutCompanyNamePipe } from './lut-company-name.pipe';

describe('LutCompanyNamePipe', () => {
  it('create an instance', () => {
    const pipe = new LutCompanyNamePipe();
    expect(pipe).toBeTruthy();
  });
});
